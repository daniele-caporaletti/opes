spring:
  application:
    name: account
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: opex
    password: bbc49577d105e63b0b92896d69e7cb55
    url: jdbc:mysql://localhost:3306/opexdb?useSSL=false&allowPublicKeyRetrieval=true
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        jdbc.time_zone: UTC
  data:
    rest:
      return-body-on-create: true
      return-body-on-update: true

server:
  port: 8080
  servlet:
    context-path: /opex/v0

